/**
 * Placeholder vocabulary set data.
 *
 * Each set groups thematically related words (Numbers, Colors, Food, etc.).
 * Populate these arrays before the vocabulary feature is implemented.
 */

import type { VocabularySet } from '@/types/vocabulary';

export const VOCABULARY_SETS: VocabularySet[] = [];
