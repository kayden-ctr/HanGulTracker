/**
 * Placeholder Hangul character data.
 *
 * This file will hold the full consonant and vowel tables.
 * For now it exports a typed empty array so imports don't break.
 */

import type { HangulCharacter } from '@/types/hangul';

/** Basic Korean consonants (자음) — populate before first release. */
export const CONSONANTS: HangulCharacter[] = [];

/** Basic Korean vowels (모음) — populate before first release. */
export const VOWELS: HangulCharacter[] = [];
